{
  "name": "cole-gannaway.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build": "cp src/index.html build/index.html && cp src/CNAME build/CNAME && cp src/native-css-styles.css build/native-css-styles.css && cp -r src/images build/images && npx tailwindcss -o build/styles.css --minify",
    "clean" : "rm -rf build/*",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.3.2"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
